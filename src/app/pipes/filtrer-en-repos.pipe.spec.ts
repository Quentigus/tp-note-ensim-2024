import { FiltrerEnReposPipe } from './filtrer-en-repos.pipe';

describe('FiltrerEnReposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerEnReposPipe();
    expect(pipe).toBeTruthy();
  });
});
